name: Deploy to Test

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to Test VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.TEST_SERVER_HOST }}
          username: ${{ secrets.TEST_SERVER_USER }}
          password: ${{ secrets.TEST_SERVER_PASSWORD }}
          command_timeout: 10m
          script: |
            set -e
            cd /opt/covered-call-engine

            echo "==> Pulling latest code..."
            git config pull.rebase false
            git fetch origin
            git reset --hard origin/develop

            echo "==> Running normalization (Emergent → VPS fixes)..."
            bash scripts/normalize_emergent.sh

            echo "==> Stopping old test containers..."
            docker compose -p cce_test --env-file .env.test -f docker/docker-compose.test.yml down --remove-orphans

            echo "==> Building and starting test containers..."
            docker compose -p cce_test --env-file .env.test -f docker/docker-compose.test.yml up -d --build

            echo "==> Waiting for containers to be ready..."
            sleep 45

            echo "==> Health check with retry..."
            for i in 1 2 3 4 5; do
              HEALTH=$(docker exec cce_test-backend-1 curl -sf http://localhost:8000/api/health 2>/dev/null || echo "FAILED")
              if echo "$HEALTH" | grep -q "healthy\|ok\|status"; then
                echo "==> Backend healthy!"
                break
              fi
              echo "==> Attempt $i/5 — retrying in 10s..."
              sleep 10
            done

            echo "==> Running smoke tests..."
            bash scripts/smoke_test.sh || echo "WARN: Smoke tests had failures — check logs"

            echo "==> Test deployment complete!"
            docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"